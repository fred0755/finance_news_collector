name: 财经新闻云端采集

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 检出代码
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 完整克隆，解决 Gitee 推送问题

      - name: 设置Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: 运行采集脚本
        id: collect
        env:
          GITEE_USERNAME: ${{ secrets.GITEE_USERNAME }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          echo "========================================="
          echo "🚀 开始采集"
          echo "========================================="
          
          # 进入采集目录并执行
          cd src/collectors
          python run_github_action.py
          
          # 返回项目根目录
          cd ../..
          
          echo ""
          echo "========================================="
          echo "📊 检查数据文件"
          echo "========================================="
          ls -la data/
          echo ""
          
          echo "========================================="
          echo "📤 同步到 Gitee"
          echo "========================================="
          
          # 配置 git
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          
          # 确保 data 目录被跟踪
          mkdir -p data
          
          # 添加并提交
          git add data/
          git diff --cached --quiet || git commit -m "chore: 自动更新财经新闻数据 $(date +'%Y-%m-%d %H:%M:%S')"
          
          # 推送到 Gitee
          if [ -n "$GITEE_USERNAME" ] && [ -n "$GITEE_TOKEN" ]; then
            echo "推送到 Gitee..."
            git push https://$GITEE_USERNAME:$GITEE_TOKEN@gitee.com/$GITEE_USERNAME/finance_news_collector.git main
            echo "✅ 同步完成"
          else
            echo "⚠️ Gitee 凭证未配置"
          fi
          
          echo ""
          echo "========================================="
          echo "🎉 完成"
          echo "========================================="

      - name: 钉钉通知
        if: always()
        env:
          DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
        run: |
          if [ -z "$DINGTALK_WEBHOOK" ]; then
            echo "⚠️ 钉钉未配置"
            exit 0
          fi
          
          STATUS="${{ job.status }}"
          TIME=$(date '+%Y-%m-%d %H:%M:%S')
          
          if [ "$STATUS" = "success" ]; then
            MSG="✅ 财经新闻云端采集成功"
            if [ -f "data/latest.json" ]; then
              COUNT=$(grep -o '"title"' data/latest.json | wc -l)
              MSG="$MSG，今日已采集 $COUNT 条新闻"
            fi
          else
            MSG="❌ 财经新闻云端采集失败"
          fi
          
          curl -s -X POST "$DINGTALK_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{
              \"msgtype\": \"text\",
              \"text\": {
                \"content\": \"$MSG\n🕐 $TIME\n🔗 https://gitee.com/${{ secrets.GITEE_USERNAME }}/finance_news_collector/tree/main/data\"
              }
            }"